#include <gtest/gtest.h>
#include "TDA.hpp"

namespace ServiceTests
{
    namespace TDA
    {
        TEST(TDATest, client_has_auth_token)
        {
            tda::Client client;

        }
    }
}